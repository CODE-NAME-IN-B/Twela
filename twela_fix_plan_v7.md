# Twela — خطة إصلاح v7 (متابعة نهائية)
> تأكدت: مشكلة السجل الرمادي انحلت بنجاح ✅ (الصورة الثالثة تعرض حركة حقيقية بتاريخ صحيح). هذا الملف يغطي 3 بنود متبقية مؤكدة.

---

## P0: صلاحية الموقع — الملف المفقود

**الملف:** `android/app/src/main/AndroidManifest.xml`
**السبب المؤكد:** الكود بـ`settings_screen.dart` صحيح (`Permission.locationWhenInUse.request()`)، بس المانفست ما يعلن الصلاحية، فأندرويد يرفضها تلقائيًا بدون ما يعرض أي نافذة.

**الإصلاح (سطرين فقط):**
```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twela.app">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>     <!-- ⬅️ جديد -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>   <!-- ⬅️ جديد -->

    <application ...>
```

**معيار القبول:** بعد التحديث، اذهب لـ Android Settings → Apps → Twela → Permissions — لازم "Location" يظهر كخيار من الأساس. اضغط "اكتشف الآن" بالتطبيق — لازم تطلع نافذة نظام حقيقية تطلب الإذن (مو رسالة رفض فورية).

---

## P1: وضع الزجاج — الكود صحيح، بس التأثير غير محسوس بصريًا

**الملف:** `lib/widgets/balance_card.dart`
**السبب:** `BackdropFilter` يطمّس المحتوى **خلف** الكارت — وخلفية الشاشة عندك مسطحة بلون واحد بدون تفاصيل، فطمسها ما يغيّر شي مرئي. الفرق الوحيد فرق شفافية بسيط جدًا (0.3 مقابل 0.5) بالكاد يُلاحظ.

**الإصلاح — أضف طبقة خلفية فيها تفاصيل خلف الكارت عشان الطمس يبين فعليًا:**
```dart
// في home_screen.dart، لف محتوى الشاشة بخلفية فيها تدرج/نقشة خفيفة
// بدل الخلفية المسطحة العادية:
Container(
  decoration: BoxDecoration(
    gradient: LinearGradient(
      begin: Alignment.topRight,
      end: Alignment.bottomLeft,
      colors: isDark
          ? [const Color(0xFF1E1B4B), const Color(0xFF0F172A)]
          : [AppColors.primary.withOpacity(0.08), Colors.white],
    ),
  ),
  child: /* محتوى الشاشة الحالي */,
)
```
بهذا الشكل، لما "وضع الزجاج" يكون مفعّل، الكارت يطمّس التدرج اللي وراه فعليًا وتشوف فرق واضح (تأثير Frosted Glass حقيقي)، بدل ما يطمّس لون مصمت.

**بديل أسرع لو ما تبي تغيّر الخلفية:** كبّر فرق الشفافية بشكل واضح أكثر (مثلاً 0.15 بدل 0.3) وزود سماكة الحدّ (`border width`) شوي — يخلي الفرق محسوس أكثر حتى بدون طمس.

**معيار القبول:** فعّل "وضع الزجاج" من الإعدادات وارجع للرئيسية — لازم تلاحظ فرق بصري واضح بكارت الرصيد، مو تغيير بالكاد يُرى.

---

## P2: تعديل/حذف الحركات بالسجل — تجربة غير موثوقة

**الملف:** `lib/widgets/transaction_tile.dart`
**الوضع الحالي:** الحذف عبر سحب (Dismissible) + حوار تأكيد، والتعديل عبر ضغط مطوّل (Long Press) يفتح تعديل الملاحظة بس (مو المبلغ أو التصنيف). كلا التفاعلين **غير مكتشفين بصريًا** (ما فيه أي إشارة بالواجهة توضح "اسحب للحذف" أو "اضغط مطولًا للتعديل")، وده سبب شائع لإحساس المستخدم إنها "أحيانًا ما تشتغل" — فعليًا يمكن تشتغل بس المستخدم ما يعرف كيف يستدعيها بشكل مضبوط (زاوية السحب، مدة الضغط).

**الإصلاح المقترح — بدّل لقائمة صريحة بدل الإيماءات المخفية:**
1. أضف أيقونة صغيرة (⋮ ثلاث نقاط) بأقصى يمين كل `TransactionTile`.
2. عند الضغط عليها، افتح `showModalBottomSheet` بخيارين واضحين: "تعديل" و"حذف" (بدل الاعتماد على السحب أو الضغط المطوّل فقط).
3. "تعديل" يفتح نفس `add_transaction_screen.dart` **معبّأة مسبقًا** بكل بيانات الحركة (المبلغ، التصنيف، الملاحظة، كاش/مصرف) — مو بس تعديل الملاحظة زي الحين، عشان لو أخطأ بالمبلغ أو التصنيف يقدر يصلحه فعليًا.
4. خلّي السحب للحذف موجود كاختصار إضافي (Power User) بدون ما يكون الطريقة الوحيدة.

**معيار القبول:** كل حركة بالسجل فيها أيقونة واضحة تفتح خيارات تعديل/حذف بدون ما تحتاج تكتشف إيماءة مخفية.

---

## الترتيب
1. **P0** (سطر المانفست) — أسرع إصلاح وأوضح أثر.
2. **P2** (قائمة تعديل/حذف صريحة) — أهم لتجربة الاستخدام اليومية.
3. **P1** (خلفية للزجاج) — تحسين بصري، مو عطل وظيفي.
