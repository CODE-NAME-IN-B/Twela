# Twela — خطة جانبية: هوية Nothing Phone + أفكار جديدة + مراجعة توافق
> هذا ملف منفصل عن خطة الإصلاح (v4). نفّذ هذا **بعد** ما تخلص من بنود P0 بخطة الإصلاح — هذا طبقة تصميم/ميزات جديدة فوق أساس شغّال، مو بديل عن إصلاح الأعطال.

---

## 1. هل ميزة "التعلم الذاتي" (يتعلم منك) موجودة فعليًا؟ — فحصتها بالكود

**الجواب: موجودة جزئيًا — النص الخلفي (Backend) شغّال، بس التجربة الفعلية للمستخدم ناقصة.**

| الجزء | الحالة |
|---|---|
| خوارزمية اكتشاف الأنماط (`routine_detection_service.dart`) | ✅ موجودة وتشتغل بعد كل حركة صرف |
| نموذج البيانات (`routine_pattern.dart`) بمراحل learning/suggesting/autoConfirmed | ✅ موجود |
| شاشة مراجعة الأنماط (`routine_settings_screen.dart`) | ✅ موجودة، تقدر تشوف الأنماط وترفعها لتلقائي أو تحذفها |
| **حفظ مفتاح تشغيل/إيقاف الميزة** | ❌ `RoutineProvider._isEnabled` بس متغير بالذاكرة، ما يُحفظ — يرجع "مفعّل" تلقائيًا كل ما تفتح التطبيق حتى لو أوقفته |
| **إشعار اقتراح فعلي عند وصول وقت النمط** ("عادة تشتري قهوة الساعة 6، سجّلها؟") | ❌ غير موجود إطلاقًا — الأنماط تتخزن بصمت، ما فيه أي إشعار يوصلك، لازم تفتح شاشة الإعدادات بنفسك عشان تشوفها |
| **التسجيل التلقائي الفعلي لما تصير حالة النمط "تلقائي"** | ❌ غير موجود — رفع النمط لـ`autoConfirmed` مجرد Tag بالبيانات، ما فيه أي كود يراقب الوقت وينشئ حركة تلقائيًا |

**خلاصة:** الميزة "نص مبنية" — فيها الذكاء (الاكتشاف) بس ما فيها التفاعل (الإشعار والتنفيذ). هذا يحتاج إصلاح منفصل (مو ضمن هذا الملف، حطه إضافة لخطة الإصلاح v4 لو حبيت — أخبرني وأضيفه).

---

## 2. هوية بصرية جديدة: Nothing Phone Design Language

أسلوب Nothing يختلف عن أسلوب Apple اللي اعتمدناه بالخطة الأولى (بطاقات ناعمة، ظلال خفيفة، ألوان دلالية متعددة). أسلوب Nothing مبني على:

- **أحادية اللون بشكل شبه كامل**: أبيض/أسود/رمادي بس، ولون تمييز واحد (نفس الأخضر `#0A846B` عندك) يظهر بأماكن محدودة جدًا ومقصودة — مو منتشر بكل مكان زي الوضع الحالي (نجاح/تحذير/خطر بألوان مختلفة).
- **خط Dot-Matrix للأرقام المهمة**: Nothing تستخدم خط "Ndot" (خط منقّط) للأرقام والعدادات. ما نقدر نستخدم نفس الخط (ملكية Nothing)، بس فيه بدائل مفتوحة بـGoogle Fonts بنفس الروح: **`Silkscreen`** أو **`DSEG7 Classic`** أو **`Micro 5`** — تُستخدم **للأرقام المالية الكبيرة بس** (الرصيد، المبالغ)، وتبقى `Inter` للنصوص العادية. هذا التبايُن (خط عادي + خط رقمي مميز) هو جوهر أسلوب Nothing.
- **حدود متقطعة (Dashed) بدل الحدود الصلبة أو الظل**: بطاقات بحدود `Dashed border` رفيعة بدل الخلفية البيضاء + ظل الحالي.
- **نقاط/شبكة خلفية خفيفة (Dot Grid)**: نمط نقطي خفيف جدًا بالخلفية (مو واضح، بس يعطي ملمس "دايركت من الهاردوير") — يشتغل بالوضعين الفاتح والداكن بشفافية منخفضة.
- **أيقونات خطوط رفيعة جدًا (Hairline icons)**: بدون تعبئة، بدون خلفيات ملونة دائرية زي الوضع الحالي.
- **الشفافية (Glyph-style)**: عناصر شبه شفافة تكشف "الطبقة اللي تحتها" — نقدر نطبقها على البطاقات الثانوية (خلفية زجاجية خفيفة `BackdropFilter` بشفافية).

---

## 3. تخطيط (Layout) جديد مقترح

### الشاشة الرئيسية
- الرصيد الإجمالي يُعرض بخط Dot-Matrix كبير جدًا (بدل الخط العادي الحالي) — نفس إحساس عداد Nothing Phone الأصلي.
- بدل شريط تقدم مستطيل عادي لصرف اليوم: صف من نقاط صغيرة (Dot Indicator) تمتلئ توًا وحدة وحدة حسب نسبة الصرف — مثال: 10 نقاط، كل نقطة = 10%.
- كارت الرصيد: خلفية شبه شفافة بحدود متقطعة رفيعة، بدل التعبئة الخضراء الكاملة الحالية (Gradient) — الأخضر يصير بس بالنص والحدود، مو خلفية كاملة.

### شريط التنقل السفلي
- الأيقونات الخمسة تتحول لخطوط رفيعة بدون خلفية بيضاوية عند التفعيل — بدلها نقطة صغيرة (Dot) تحت الأيقونة النشطة بس (زي مؤشرات Nothing OS).
- زر "+" يصير دائرة بحدود بس (Outline) بدل التعبئة الكاملة، مع علامة "+" بخط رفيع.

### قائمة الحركات (السجل)
- كل حركة تُعرض بشكل أقرب لـ"إيصال" (Receipt strip): خط فاصل متقطع بين كل حركة والثانية بدل الفراغ العادي، والمبلغ بخط Dot-Matrix.

### الودجات (Home Screen Widgets)
- تصميمها يطابق نفس الروح (أرقام Dot-Matrix، خلفية معتمة شبه شفافة) — بما إنها أصلًا بمرحلة مستقبلية بالخطة الأساسية، دمج هالهوية فيها من البداية أسهل من إعادة تصميمها بعدين.

---

## 4. مراجعة توافق الكود الحالي مع هذا التوجه الجديد

فحصت `lib/theme/app_colors.dart` والشاشات الحالية، وهذا وضع التوافق:

| العنصر | الوضع الحالي | يحتاج تغيير؟ |
|---|---|---|
| لوحة الألوان | Slate متعددة الأسطح (`#0F172A`, `#1E293B`, ألوان دلالية success/warning/danger منفصلة) | ✅ نعم — تبسيط لأحادية اللون + تقليل استخدام الألوان الدلالية لأماكن حرجة بس |
| الخطوط | `google_fonts` (Inter) للكل | ✅ نعم — إضافة خط Dot-Matrix ثانوي للأرقام بس، مو استبدال كامل |
| البطاقات | `BorderRadius.circular` + `Border.all` صلب رفيع (بدون ظل تقريبًا) | 🟡 جزئي — الأساس قريب (بدون ظل ثقيل زي ما طلبنا بالخطة الأولى)، بس الحدود صلبة لازم تصير متقطعة |
| التنقل السفلي | خلفية بيضاوية ملونة خلف الأيقونة النشطة | ✅ نعم — تبديل لمؤشر نقطة بسيط |
| الرصيد بالرئيسية | كارت Gradient أخضر معبّى بالكامل | ✅ نعم — تبديل لكارت شفاف بحدود + نص Dot-Matrix |
| الأيقونات | `Icons.xxx` من Material، بخلفيات دائرية ملونة لكل تصنيف | 🟡 جزئي — الأيقونات نفسها تقدر تفضل (خط رفيع أصلًا)، بس شيل الخلفيات الملونة الدائرية |

**الخلاصة:** الأساس الحالي (بطاقات نظيفة بدون ظل ثقيل، فراغات كافية) **قاعدة جيدة نبني عليها**، مو نبدأ من الصفر. التغيير الأكبر هو: تقليل الألوان لدرجة أحادية + إضافة خط Dot-Matrix للأرقام + تحويل الحدود لمتقطعة + تبسيط التنقل السفلي.

**توصية تنفيذ:** سوّها كـ"ثيم بديل" (`AppTheme.nothingTheme()`) جنب الثيم الحالي، مو استبدال مباشر — تقدر تسويها Toggle بالإعدادات "المظهر الكلاسيكي / مظهر Nothing" لو حبيت تجرب قبل ما تلتزم بيها كليًا.

---

## 5. فكرة جديدة: تغيير العملة تلقائيًا حسب الموقع (GPS)

الفكرة نفسها منطقية (مسافر أو انتقل لبلد ثاني)، بس فيها نقطة تحتاج انتباه قبل التنفيذ:

**⚠️ ملاحظة مهمة:** طلب صلاحية GPS بشكل دائم لمجرد "تحديد العملة" سبب شائع لرفض التطبيقات بمراجعة Google Play/App Store (صلاحية الموقع تعتبر حساسة، وتحتاج تبرير واضح ومباشر مرتبط بوظيفة أساسية). بما إن Twela أساسًا مبني حول الدينار الليبي، التوصية:

1. **الافتراضي بدون أي صلاحية**: استخدم `Platform.localeName` أو رمز الدولة من الـSIM (`sim_country_code` عبر مكتبة بسيطة) لتحديد العملة المقترحة تلقائيًا — بدون طلب أي إذن حساس.
2. **GPS اختياري بس، بزر صريح**: "اكتشف عملتي حسب موقعي" — زر يدوي المستخدم يضغطه بنفسه لما يحب، مو صلاحية دائمة بالخلفية. هذا يقلل احتكاك المراجعة وويحترم خصوصية المستخدم أكثر.
3. **العملة الأساسية تبقى دائمًا قابلة للتغيير يدويًا** من الإعدادات، بغض النظر عن الاكتشاف التلقائي — لا تفرض تغيير العملة بصمت.
4. **تحتاج مصدر بيانات لأسعار الصرف** (API خارجي مثل exchangerate.host) — يحتاج إنترنت، وتخزين آخر سعر معروف محليًا للعمل بدون نت.

**نموذج بيانات مقترح:**
```
lib/models/currency_settings.dart
- primaryCurrency: String (كود العملة، افتراضي 'LYD')
- displaySecondaryCurrency: String? (اختياري، للمسافرين)
- lastKnownExchangeRate: double?
- lastRateUpdate: DateTime?
```

---

## 6. الرسائل تحت الشعار — نبرة داعمة إيجابية

**الوضع الحالي** (فحصته بـ`onboarding_screen.dart`): النص وظيفي بحت — "تتبع مصاريفك بسهولة بالدينار الليبي". ما فيه أي نبرة عاطفية أو تشجيعية.

**المقترح:** مجموعة رسائل قصيرة داعمة، تتغيّر عشوائيًا كل ما يفتح المستخدم التطبيق (splash) أو onboarding — النبرة: غير حكمية، متفهمة، تشجّع بدون ضغط أو شعور بالذنب حول المصاريف. أمثلة:
- "كل خطوة صغيرة بالتسجيل تقربك لصورة أوضح عن فلوسك"
- "ما فيه حكم هنا، بس فهم أفضل ليومك المالي"
- "تتبعك اليوم هو أساس راحة بالك بكرة"
- "خطوة بخطوة، أنت أقرب لأهدافك المالية"
- "كل دينار تسجّله يقربك لصورة أوضح"

**التنفيذ:** قائمة نصوص ثابتة بالكود (`lib/utils/motivational_messages.dart`)، تختار وحدة عشوائية عند فتح شاشة الترحيب أو الرئيسية أول مرة باليوم. لاحقًا ممكن تربطها بحالة المستخدم (لو قريب يكمل هدف ادخار، رسالة مختلفة عن لو تجاوز ميزانيته).

---

## 7. أفكار إضافية جديدة (مو مذكورة قبل)

- **شريط "Glyph" علوي بالرئيسية**: سطر رفيع فوق كارت الرصيد يعرض معلومة سريعة متغيّرة (مو إشعار، بس معلومة سلبية): "بقيت 6 أيام على نهاية الشهر" أو "وصلت 70% من ميزانيتك".
- **الوضع الشفاف (Glass Mode)**: خيار بالإعدادات يخلي البطاقات الثانوية شبه شفافة (`BackdropFilter` بلور خفيف) — يفعّل فقط لو أداء الجهاز يسمح (تجنب البطء بالأجهزة الضعيفة).
- **صوت/اهتزاز خفيف عند الحفظ**: نقرة اهتزاز بسيطة (Haptic Feedback) عند حفظ أي عملية — لمسة "فيزيائية" تتماشى مع فلسفة Nothing بالتفاعل الملموس.
- **تخصيص لون التمييز**: بدل ما يكون الأخضر ثابت للجميع، خلي المستخدم يقدر يختار من 3-4 ألوان تمييز بديلة (نفس مبدأ Nothing OS اللي يخليك تخصص الألوان) — يبقى نفس مبدأ "لون وحد بس"، بس المستخدم يختاره.

---

## ترتيب تنفيذ هذا الملف (بعد إصلاحات v4)
1. أضف الخط الرقمي (Dot-Matrix) كخط ثانوي — أسهل تغيير وأوضح أثر بصري فوري.
2. بسّط لوحة الألوان لأحادية + لون تمييز واحد.
3. بدّل الحدود لمتقطعة + شيل الظلال المتبقية.
4. أعد تصميم التنقل السفلي (نقطة بدل خلفية).
5. الرسائل الداعمة تحت الشعار — تنفيذ سريع ومنفصل، ممكن تسويه بأي وقت بدون ما ينتظر باقي البنود.
6. ميزة العملة عبر الموقع — آخر شي، تحتاج تصميم API خارجي وتخزين أسعار الصرف.
